import { defineStyleConfig } from "@chakra-ui/react";

export const defaultCard = defineStyleConfig({
    variants: {
        outline: {
            container: {
                borderColor: "#457B9D",
            },
        },
    },
});
